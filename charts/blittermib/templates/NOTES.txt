blittermib {{ .Chart.AppVersion }} is deploying as release "{{ .Release.Name }}".

The MIB corpus is re-ingested on startup (~5s), so give the pod a moment
to become Ready:

  kubectl -n {{ .Release.Namespace }} rollout status deploy/{{ include "blittermib.fullname" . }}

Reach the web UI:
{{- if .Values.ingress.enabled }}
{{- range .Values.ingress.hosts }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ .host }}/
{{- end }}
{{- else if .Values.httpRoute.enabled }}
  Via the Gateway referenced in httpRoute.parentRefs
  {{- with .Values.httpRoute.hostnames }} (hostnames: {{ join ", " . }}){{- end }}.
{{- else }}
  No ingress/httpRoute enabled — port-forward to reach it locally:

  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "blittermib.fullname" . }} 8080:{{ .Values.service.port }}
  # then open http://localhost:8080
{{- end }}

{{- if and .Values.uploads.enabled (not .Values.persistence.enabled) }}

WARNING: uploads.enabled is set but persistence.enabled is not — uploaded
MIBs live in an emptyDir and will be lost when the pod is replaced. Enable
persistence to keep them.
{{- end }}
