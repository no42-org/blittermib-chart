apiVersion: v1
kind: Service
metadata:
  name: {{ include "blittermib.fullname" . }}
  labels:
    {{- include "blittermib.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "blittermib.selectorLabels" . | nindent 4 }}
